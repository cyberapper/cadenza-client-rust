# WsRpcRequestData

## Enum Variants

| Name | Description |
|---- | -----|
| RpcCancelTradeOrderParams | RPC request payload. Schema depends on the method being called. |
| RpcConnectTradingAccountParams | RPC request payload. Schema depends on the method being called. |
| RpcCreateCredentialParams | RPC request payload. Schema depends on the method being called. |
| RpcCreateSubscriptionParams | RPC request payload. Schema depends on the method being called. |
| RpcCreateTradingAccountParams | RPC request payload. Schema depends on the method being called. |
| RpcDeleteInstrumentParams | RPC request payload. Schema depends on the method being called. |
| RpcDeleteSecuritiesParams | RPC request payload. Schema depends on the method being called. |
| RpcDeleteTradingAccountParams | RPC request payload. Schema depends on the method being called. |
| RpcDisableInstrumentParams | RPC request payload. Schema depends on the method being called. |
| RpcDisableTradingAccountParams | RPC request payload. Schema depends on the method being called. |
| RpcDisconnectTradingAccountParams | RPC request payload. Schema depends on the method being called. |
| RpcEnableInstrumentParams | RPC request payload. Schema depends on the method being called. |
| RpcEnableTradingAccountParams | RPC request payload. Schema depends on the method being called. |
| RpcGetOrderBookParams | RPC request payload. Schema depends on the method being called. |
| RpcGetPortfolioParams | RPC request payload. Schema depends on the method being called. |
| RpcGetTradeOrderByIdParams | RPC request payload. Schema depends on the method being called. |
| RpcGetTradeOrderParams | RPC request payload. Schema depends on the method being called. |
| RpcListCredentialsParams | RPC request payload. Schema depends on the method being called. |
| RpcListInstrumentsParams | RPC request payload. Schema depends on the method being called. |
| RpcListKlinesParams | RPC request payload. Schema depends on the method being called. |
| RpcListOrderBookSubscriptionsParams | RPC request payload. Schema depends on the method being called. |
| RpcListOrderBooksParams | RPC request payload. Schema depends on the method being called. |
| RpcListPortfoliosParams | RPC request payload. Schema depends on the method being called. |
| RpcListSecuritiesParams | RPC request payload. Schema depends on the method being called. |
| RpcListSubscriptionsParams | RPC request payload. Schema depends on the method being called. |
| RpcListTickersParams | RPC request payload. Schema depends on the method being called. |
| RpcListTradeOrdersParams | RPC request payload. Schema depends on the method being called. |
| RpcListTradingAccountOperationsParams | RPC request payload. Schema depends on the method being called. |
| RpcListTradingAccountSubscriptionsParams | RPC request payload. Schema depends on the method being called. |
| RpcListTradingAccountsParams | RPC request payload. Schema depends on the method being called. |
| RpcRevokeCredentialParams | RPC request payload. Schema depends on the method being called. |
| RpcRotateCredentialParams | RPC request payload. Schema depends on the method being called. |
| RpcSaveCredentialParams | RPC request payload. Schema depends on the method being called. |
| RpcSaveInstrumentsParams | RPC request payload. Schema depends on the method being called. |
| RpcSaveKlinesParams | RPC request payload. Schema depends on the method being called. |
| RpcSaveOrderBooksParams | RPC request payload. Schema depends on the method being called. |
| RpcSavePortfolioParams | RPC request payload. Schema depends on the method being called. |
| RpcSaveSecuritiesParams | RPC request payload. Schema depends on the method being called. |
| RpcSaveTickersParams | RPC request payload. Schema depends on the method being called. |
| RpcSaveTradeOrderParams | RPC request payload. Schema depends on the method being called. |
| RpcSaveTradingAccountsParams | RPC request payload. Schema depends on the method being called. |
| RpcSubmitTradeOrderParams | RPC request payload. Schema depends on the method being called. |
| RpcSubscribeOrderBookParams | RPC request payload. Schema depends on the method being called. |
| RpcSubscribeTradingAccountStreamParams | RPC request payload. Schema depends on the method being called. |
| RpcSyncInstrumentsParams | RPC request payload. Schema depends on the method being called. |
| RpcSyncSecuritiesParams | RPC request payload. Schema depends on the method being called. |
| RpcUnsubscribeOrderBookParams | RPC request payload. Schema depends on the method being called. |
| RpcUnsubscribeTradingAccountStreamParams | RPC request payload. Schema depends on the method being called. |
| RpcUpdateTradingAccountParams | RPC request payload. Schema depends on the method being called. |
| RpcValidateTradingAccountParams | RPC request payload. Schema depends on the method being called. |
| RpcVerifyCredentialParams | RPC request payload. Schema depends on the method being called. |
| serde_json::Value | RPC request payload. Schema depends on the method being called. |

[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


