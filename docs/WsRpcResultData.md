# WsRpcResultData

## Enum Variants

| Name | Description |
|---- | -----|
| RpcCancelTradeOrderResult | RPC response payload. Schema depends on the method that was called. |
| RpcConnectTradingAccountResult | RPC response payload. Schema depends on the method that was called. |
| RpcCreateCredentialResult | RPC response payload. Schema depends on the method that was called. |
| RpcCreateSubscriptionResult | RPC response payload. Schema depends on the method that was called. |
| RpcCreateTradingAccountResult | RPC response payload. Schema depends on the method that was called. |
| RpcDeleteInstrumentResult | RPC response payload. Schema depends on the method that was called. |
| RpcDeleteSecuritiesResult | RPC response payload. Schema depends on the method that was called. |
| RpcDeleteTradingAccountResult | RPC response payload. Schema depends on the method that was called. |
| RpcDisableInstrumentResult | RPC response payload. Schema depends on the method that was called. |
| RpcDisableTradingAccountResult | RPC response payload. Schema depends on the method that was called. |
| RpcDisconnectTradingAccountResult | RPC response payload. Schema depends on the method that was called. |
| RpcEnableInstrumentResult | RPC response payload. Schema depends on the method that was called. |
| RpcEnableTradingAccountResult | RPC response payload. Schema depends on the method that was called. |
| RpcGetOrderBookResult | RPC response payload. Schema depends on the method that was called. |
| RpcGetPortfolioResult | RPC response payload. Schema depends on the method that was called. |
| RpcGetSystemInfoResult | RPC response payload. Schema depends on the method that was called. |
| RpcGetTradeOrderByIdResult | RPC response payload. Schema depends on the method that was called. |
| RpcGetTradeOrderResult | RPC response payload. Schema depends on the method that was called. |
| RpcHealthCheckResult | RPC response payload. Schema depends on the method that was called. |
| RpcListCredentialsResult | RPC response payload. Schema depends on the method that was called. |
| RpcListInstrumentsResult | RPC response payload. Schema depends on the method that was called. |
| RpcListKlinesResult | RPC response payload. Schema depends on the method that was called. |
| RpcListOrderBookSubscriptionsResult | RPC response payload. Schema depends on the method that was called. |
| RpcListOrderBooksResult | RPC response payload. Schema depends on the method that was called. |
| RpcListPortfoliosResult | RPC response payload. Schema depends on the method that was called. |
| RpcListSecuritiesResult | RPC response payload. Schema depends on the method that was called. |
| RpcListSubscriptionsResult | RPC response payload. Schema depends on the method that was called. |
| RpcListTickersResult | RPC response payload. Schema depends on the method that was called. |
| RpcListTradeOrdersResult | RPC response payload. Schema depends on the method that was called. |
| RpcListTradingAccountOperationsResult | RPC response payload. Schema depends on the method that was called. |
| RpcListTradingAccountSubscriptionsResult | RPC response payload. Schema depends on the method that was called. |
| RpcListTradingAccountsResult | RPC response payload. Schema depends on the method that was called. |
| RpcListVenuesResult | RPC response payload. Schema depends on the method that was called. |
| RpcRevokeCredentialResult | RPC response payload. Schema depends on the method that was called. |
| RpcRotateCredentialResult | RPC response payload. Schema depends on the method that was called. |
| RpcSaveCredentialResult | RPC response payload. Schema depends on the method that was called. |
| RpcSaveInstrumentsResult | RPC response payload. Schema depends on the method that was called. |
| RpcSaveKlinesResult | RPC response payload. Schema depends on the method that was called. |
| RpcSaveOrderBooksResult | RPC response payload. Schema depends on the method that was called. |
| RpcSavePortfolioResult | RPC response payload. Schema depends on the method that was called. |
| RpcSaveSecuritiesResult | RPC response payload. Schema depends on the method that was called. |
| RpcSaveTickersResult | RPC response payload. Schema depends on the method that was called. |
| RpcSaveTradeOrderResult | RPC response payload. Schema depends on the method that was called. |
| RpcSaveTradingAccountsResult | RPC response payload. Schema depends on the method that was called. |
| RpcSubmitTradeOrderResult | RPC response payload. Schema depends on the method that was called. |
| RpcSubscribeOrderBookResult | RPC response payload. Schema depends on the method that was called. |
| RpcSubscribeTradingAccountStreamResult | RPC response payload. Schema depends on the method that was called. |
| RpcSyncInstrumentsResult | RPC response payload. Schema depends on the method that was called. |
| RpcSyncSecuritiesResult | RPC response payload. Schema depends on the method that was called. |
| RpcUnsubscribeOrderBookResult | RPC response payload. Schema depends on the method that was called. |
| RpcUnsubscribeTradingAccountStreamResult | RPC response payload. Schema depends on the method that was called. |
| RpcUpdateTradingAccountResult | RPC response payload. Schema depends on the method that was called. |
| RpcValidateTradingAccountResult | RPC response payload. Schema depends on the method that was called. |
| RpcVerifyCredentialResult | RPC response payload. Schema depends on the method that was called. |

[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


